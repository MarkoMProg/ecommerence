# Build artefacts
dist/
node_modules/
.turbo/

# Environment files – passed via env_file in docker-compose
.env
.env.local
.env.*.local

# NOTE: public/uploads/ is included in the build so catalog assets under uploads/products/
# ship with the image. Compose may still mount a named volume over /app/public/uploads —
# then seed the volume or adjust compose if those files must appear at runtime.

# Test / coverage
test/
coverage/
**/*.spec.ts
**/*.e2e-spec.ts

# Source maps
**/*.map
